# Flask Configuration
SECRET_KEY=your-secret-key-here
DEBUG=True

# Model Configuration
DEFAULT_MODEL=model.gguf

# LLM Settings
N_CTX=4096      # Context window size
N_GPU_LAYERS=-1 # 0 = CPU only, -1 = all layers on GPU
N_THREADS=16    # Number of CPU threads

# Generation Defaults
MAX_TOKENS=512
TEMPERATURE=0.7
TOP_P=0.9
TOP_K=40
